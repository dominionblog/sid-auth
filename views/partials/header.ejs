<!DOCTYPE html>
<html>

<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, shrink-to-fit=no" />
    <title>snippets</title>
    <link rel="stylesheet" href="/module/bootstrap/dist/css/bootstrap.min.css" />
    <link rel="stylesheet" href="/module/font-awesome/css/font-awesome.min.css" />
    <link rel="stylesheet" href="/css/index.css" />
    <link rel="stylesheet" href="/css/signin.css" />
    <link rel="stylesheet" href="/css/new.css" />
    <link rel="stylesheet" href="/css/feed.css" />
    <link rel="stylesheet" href="/css/styles.css" />
</head>

<body>
    <div class="navigation">
        <div class="div-logo">
            <h1>Snippets</h1>
        </div>
        <% if (res.locals.isLoggedIn) { %> 
            <div class="navigation-buttons">
                <a href="/snippets/feed">
                    <div class="<%= res.locals.page == 'feed' ? 'active' : null %> feed-btn navigation-button btn btn-primary">
                        <h2>Feed<i class="fa fa-search"></i></h2>
                    </div>
                </a>
                <a href="/snippets/new">
                    <div class="<%= res.locals.page == 'new' ? 'active' : null %> new-btn navigation-button btn btn-primary">
                        <h2>New<i class="fa fa-plus-circle"></i></h2>
                    </div>
                </a>
            </div>
        <% } %>
        <div class="account-info">
            <% if(res.locals.isLoggedIn) { %>
                <h2>Logged in as</h2>
                <h2 class="username-display"><%= res.locals.user.username %></h2>
                <span>
                    <a href="/auth/logout" class="btn btn-primary" role="button">Logout</a>
                </span>
            <% } else { %>
                <h2>Not Logged In</h2>
                <span>
                    <a href="/auth/login" class="btn btn-primary" role="button">Log In</a>
                </span>
            <% } %>
        </div>
    </div>